# Copy to .env.local and set to your board's WebSocket endpoint.
# The ESP32 serves this itself at /ws - no MQTT broker or websocket
# listener config needed, this connects straight to the device.
VITE_DEFAULT_DEVICE_URL=ws://lr2redux.local/ws
