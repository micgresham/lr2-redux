// Copy this file to secrets.h and fill in your own value.
// secrets.h is for local use only - do not commit it.
//
// WiFi and MQTT are no longer set here - they're runtime config, entered
// through the setup portal (SoftAP + captive portal on first boot, or hold
// the cycle button 10s to re-enter it). See README.md.

#pragma once

#define OTA_PASSWORD  "change-me" // used for `pio run -t upload --upload-port <ip>` over WiFi
